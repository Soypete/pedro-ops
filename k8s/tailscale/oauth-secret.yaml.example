# Tailscale OAuth Secret
# DO NOT commit this file with real credentials!
# Copy this to oauth-secret.yaml and fill in your OAuth credentials

apiVersion: v1
kind: Secret
metadata:
  name: tailscale-operator-oauth
  namespace: tailscale
type: Opaque
stringData:
  client_id: "YOUR_TAILSCALE_CLIENT_ID"
  client_secret: "YOUR_TAILSCALE_CLIENT_SECRET"

---
# Instructions:
# 1. Go to Tailscale admin console: https://login.tailscale.com/admin/settings/oauth
# 2. Create new OAuth client
# 3. Copy Client ID and Client Secret
# 4. Create oauth-secret.yaml from this template:
#    cp oauth-secret.yaml.example oauth-secret.yaml
# 5. Replace YOUR_TAILSCALE_CLIENT_ID and YOUR_TAILSCALE_CLIENT_SECRET
# 6. Apply: kubectl apply -f oauth-secret.yaml
# 7. NEVER commit oauth-secret.yaml to git!
