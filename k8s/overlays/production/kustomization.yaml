apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Reference base manifests
bases:
  - ../../base

# Production-specific namespace
namespace: pedro-ops

# Production labels
commonLabels:
  environment: production
  cluster: pedro-ops-cluster

# Production annotations
commonAnnotations:
  managed-by: foundry-cli
  cluster.pedro-ops.io/environment: production

# Resources specific to production
resources:
  # Add production-specific resources here
  # Example:
  # - ingress.yaml
  # - certificates.yaml

# ConfigMap generator for production configs
# configMapGenerator:
#   - name: app-config
#     literals:
#       - LOG_LEVEL=info
#       - ENVIRONMENT=production

# Secret generator (use sealed-secrets or external-secrets in production)
# secretGenerator:
#   - name: app-secrets
#     envs:
#       - secrets.env

# Patches for production-specific modifications
# patchesStrategicMerge:
#   - deployment-patch.yaml

# JSON patches for targeted modifications
# patchesJson6902:
#   - target:
#       group: apps
#       version: v1
#       kind: Deployment
#       name: my-app
#     path: replica-count-patch.yaml
